version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused

issues:
  exclude-rules:
    # Test helpers and table-driven tests routinely shadow err.
    - path: "_test\\.go"
      linters:
        - errcheck
